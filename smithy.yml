project: python
runtime: 2.7

requirements:
  - requirements.txtt

env:
  - LIKES=PIES
  - EATS=TASTY_PIES
  - EAT_PIES_SLOWLY=1

script:
  - nosetests
  - python setup.py sdist

test_reports:
  - test-xml/*.xml
